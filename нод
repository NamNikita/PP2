#include <iostream>

using namespace std;

int main() {

    int b, d;
   
    for (int i = b; i > 0; i--) {
        if (b % i == 0 && b % d == 0) {
            cout << "nod = " << i;
            break;
        }
    }
    return 0;
}
